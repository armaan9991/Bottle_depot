import Login from "../pages/Login";
import API from "./axios";

export default function Login(WorkId,password){
    return API.post()         /* // this is where we will send our credentials to check that we need to have 
    // will return the body in JSON of data that we need.
    need to write API call  to get user infomation..
    */
}